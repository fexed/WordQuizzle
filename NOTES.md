## Note sul progetto
### Protocollo client-server
##### Scambio di messaggi `<tipo>:<dati>`. Messaggi:
 * `challenge:<nickAmico>`
 * `login:<nickUtente> <password>`
 * `showonline`
 * `addfriend:<nickAmico>`
 * `friendlist`
 * `points`
 * `ranking`